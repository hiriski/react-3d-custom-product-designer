// components
import Box from '@mui/material/Box'
import Typography from '@mui/material/Typography'

const Designer = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        width: '100%',
        paddingRight: '500px',
      }}
    >
      <Typography>Put designer content here</Typography>
    </Box>
  )
}

export default Designer
